# Enable CORS globally (optional, since we already use @CrossOrigin in controller)
# server.servlet.context-path=/api

# Server port (optional)
server.port=8080

# Logging level (optional)
logging.level.org.springframework=INFO
